{
  "name": "kairos-signaling-server",
  "version": "1.0.0",
  "description": "Token issuer and management server for KAIROS LiveKit sessions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "livekit-server-sdk": "^2.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
